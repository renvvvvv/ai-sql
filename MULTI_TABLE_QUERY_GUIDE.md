# 🔗 多表联查功能使用指南

## 📋 功能概述

BS SQL 现已支持多表联查功能，可以智能地分析多个表格的关联关系，生成跨表查询的 SQL 语句，实现更强大的数据分析能力。

## 🚀 使用流程

### 步骤一：开启多表联查模式
1. 在主界面顶部找到 **"单表查询模式"** 按钮
2. 点击切换为 **"多表联查模式"**
3. 界面会显示橙色的多表联查提示

### 步骤二：查看表结构（推荐）
1. 点击 **"查看表结构"** 按钮
2. 系统会显示所有表格的详细结构信息
3. 了解表格间的字段关联关系
4. 查看每个表格的字段数量和类型

### 步骤三：输入自然语言查询
在输入框中描述你的查询需求，例如：
- "统计各个区域在所有表格中的UPS负载率平均值"
- "查询所有表格中销售额大于1000的订单信息"
- "合并多个表格的客户数据，按地区分组统计"

### 步骤四：生成多表联查SQL
1. 点击 **"多表联查"** 按钮（橙色）
2. 系统会自动：
   - 分析表格结构和关联关系
   - 生成跨表查询SQL语句
   - 执行查询并显示结果

### 步骤五：查看执行结果
- 查看查询结果表格
- 检查生成的SQL语句
- 查看详细的执行日志

## 🔧 多表联查策略

### 1. UNION 合并策略
当多个表格具有相同的字段结构时，系统会使用 UNION 合并数据：
```sql
SELECT 区域, UPS负载率 FROM 表格1
UNION ALL
SELECT 区域, UPS负载率 FROM 表格2
UNION ALL
SELECT 区域, UPS负载率 FROM 表格3
```

### 2. JOIN 连接策略
当表格间存在关联字段时，系统会使用 JOIN 连接数据：
```sql
SELECT t1.客户名称, t2.订单金额
FROM 客户表 t1
JOIN 订单表 t2 ON t1.客户ID = t2.客户ID
```

### 3. 子查询策略
对于复杂的条件筛选，系统会生成子查询：
```sql
SELECT * FROM 表格1 
WHERE 字段1 IN (SELECT 字段1 FROM 表格2 WHERE 条件)
```

## 💡 使用技巧

### 查询描述建议
- **明确指定需要的表格**："在UPS表格和设备表格中查询..."
- **描述关联关系**："根据设备ID关联两个表格"
- **指定聚合需求**："统计各个区域的平均值"
- **说明筛选条件**："只查询负载率大于50%的设备"

### 常见查询模式
1. **数据合并**："合并所有表格中的区域数据"
2. **关联查询**："根据ID字段关联设备表和监控表"
3. **统计分析**："统计各个表格中不同类型设备的数量"
4. **条件筛选**："查询所有表格中满足条件的记录"

## 🎯 实际应用示例

### 示例1：区域负载率统计
**查询**："统计各个区域在所有UPS表格中的负载率平均值"

**生成的SQL**：
```sql
SELECT 区域, AVG(UPS负载率) as 平均负载率
FROM (
  SELECT fldgdKthw6 as 区域, fldqdhDha6 as UPS负载率 FROM tblCJZRo6RPNsiZh
  UNION ALL
  SELECT fldgdKthw6 as 区域, fldqdhDha6 as UPS负载率 FROM tblvUW1yZ349IlBE
  UNION ALL
  SELECT fldgdKthw6 as 区域, fldqdhDha6 as UPS负载率 FROM tbl7394Mjeu90xAJ
) 
WHERE 区域 IS NOT NULL
GROUP BY 区域;
```

### 示例2：设备关联查询
**查询**："根据设备ID关联基础信息表和监控数据表"

**生成的SQL**：
```sql
SELECT 
  t1.设备名称,
  t1.设备型号,
  t2.监控数值,
  t2.监控时间
FROM 设备基础信息表 t1
JOIN 设备监控数据表 t2 ON t1.设备ID = t2.设备ID
WHERE t2.监控时间 >= '2024-01-01'
```

## 🔍 调试和优化

### 查看执行日志
1. 点击 **"查看日志"** 按钮
2. 筛选 **"AI请求"** 和 **"SQL执行"** 日志
3. 查看详细的执行过程和错误信息

### 常见问题解决
1. **查询结果为空**：检查表格间是否有共同字段
2. **SQL语法错误**：查看日志中的错误信息，手动调整SQL
3. **执行超时**：减少查询的表格数量或添加筛选条件

### 手动优化SQL
如果AI生成的SQL不够理想，可以：
1. 复制生成的SQL到编辑器
2. 手动修改和优化
3. 点击 **"Query"** 按钮执行

## 🎨 界面功能说明

### 模式切换区域
- **多表联查模式按钮**：切换查询模式
- **查看表结构按钮**：显示所有表格的详细信息
- **模式提示文本**：显示当前模式和功能说明

### 查询输入区域
- **自然语言输入框**：输入查询需求（支持中英文）
- **多表联查按钮**：生成并执行多表查询SQL
- **单表查询按钮**：生成单表查询SQL（在多表模式下）
- **Query按钮**：执行当前SQL语句

### 结果显示区域
- **查询结果表格**：显示查询结果
- **SQL编辑器**：显示和编辑生成的SQL
- **执行日志**：显示详细的执行过程

## 📊 性能说明

### 查询性能
- **小数据集** (< 1000 记录): < 200ms
- **中等数据集** (1000-10000 记录): < 1s
- **大数据集** (> 10000 记录): < 5s
- **多表联查**: 根据表格数量和数据量而定

### 内存使用
- **基础运行**: ~50MB
- **多表数据加载**: ~200-500MB
- **复杂联查**: ~1GB

## 🔮 未来功能

- [ ] 可视化表格关系图
- [ ] 智能查询建议
- [ ] 查询性能优化
- [ ] 自定义表格别名
- [ ] 查询结果缓存

---

**多表联查功能让数据分析更加强大和灵活！** 🚀